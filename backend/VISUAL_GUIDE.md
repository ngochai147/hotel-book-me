# 📊 Visual Guide - Backend Architecture

## 🗂️ Folder Structure Visualization

```
backend/
│
├── 📋 Configuration & Setup
│   ├── .env.example          → Template for environment variables
│   ├── .gitignore            → Files to ignore in Git
│   ├── package.json          → Dependencies & scripts
│   ├── render.yaml           → Render deployment config
│   └── server.js             → 🚀 Application entry point
│
├── ⚙️ config/
│   ├── database.js           → MongoDB connection handler
│   ├── firebaseConfig.js     → Firebase initialization
│   └── serviceAccountKey.js  → Firebase credentials
│
├── 🎮 controllers/           → Business Logic
│   ├── authController.js     → 🔐 Auth operations
│   ├── hotelController.js    → 🏨 Hotel CRUD
│   ├── bookingController.js  → 📅 Booking management
│   ├── reviewController.js   → ⭐ Review system
│   └── userController.js     → 👤 User profile
│
├── 🛡️ middlewares/           → Request Processing
│   ├── errorHandler.js       → 🚨 Global error handling
│   ├── logger.js             → 📝 Request logging
│   └── verifyToken.js        → 🔑 Auth verification
│
├── 📦 models/                → Database Schemas
│   ├── User.js               → 👤 User model
│   ├── Hotel.js              → 🏨 Hotel model
│   ├── Booking.js            → 📅 Booking model
│   ├── Review.js             → ⭐ Review model
│   └── Room.js               → 🛏️ Room model
│
├── 🛣️ routes/                → API Endpoints
│   ├── authRoutes.js         → /api/auth/*
│   ├── hotelRoutes.js        → /api/hotels/*
│   ├── bookingRoutes.js      → /api/bookings/*
│   ├── reviewRoutes.js       → /api/reviews/*
│   └── userRoutes.js         → /api/users/*
│
└── 📚 Documentation
    ├── README.md             → Project overview
    ├── ARCHITECTURE.md       → Architecture details
    ├── DEPLOYMENT_GUIDE.md   → Step-by-step deployment
    ├── API_DOCUMENTATION.js  → API reference
    └── SETUP_COMPLETE.md     → Setup summary
```

## 🔄 Request Flow Diagram

### Public Request (Get Hotels)

```
┌─────────────┐
│   Client    │
│  (Frontend) │
└──────┬──────┘
       │ GET /api/hotels?location=hanoi
       │
       ▼
┌─────────────────────────────────────────┐
│          Express Server                  │
│  ┌────────────────────────────────┐    │
│  │  1. CORS Middleware             │    │
│  └────────────┬───────────────────┘    │
│               │                         │
│  ┌────────────▼───────────────────┐    │
│  │  2. JSON Parser                 │    │
│  └────────────┬───────────────────┘    │
│               │                         │
│  ┌────────────▼───────────────────┐    │
│  │  3. Logger (dev only)           │    │
│  └────────────┬───────────────────┘    │
│               │                         │
│  ┌────────────▼───────────────────┐    │
│  │  4. Route: /api/hotels          │    │
│  └────────────┬───────────────────┘    │
└───────────────┼─────────────────────────┘
                │
                ▼
┌───────────────────────────────────────┐
│   hotelController.getAllHotels()      │
│  ┌──────────────────────────────┐    │
│  │ • Parse query params          │    │
│  │ • Build MongoDB query         │    │
│  │ • Apply filters & pagination  │    │
│  │ • Execute query               │    │
│  └──────────┬───────────────────┘    │
└─────────────┼───────────────────────────┘
              │
              ▼
┌─────────────────────────┐
│   MongoDB Atlas         │
│   Query: hotels         │
└──────────┬──────────────┘
           │
           │ [Hotel documents]
           │
           ▼
┌────────────────────────┐
│   Response             │
│   {                    │
│     success: true,     │
│     count: 10,         │
│     data: [...]        │
│   }                    │
└──────────┬─────────────┘
           │
           ▼
     ┌─────────┐
     │ Client  │
     └─────────┘
```

### Authenticated Request (Create Booking)

```
┌─────────────┐
│   Client    │
│  (Frontend) │
└──────┬──────┘
       │ POST /api/bookings
       │ Header: Authorization: Bearer <token>
       │ Body: { hotelId, checkIn, checkOut... }
       │
       ▼
┌─────────────────────────────────────────────┐
│          Express Server                      │
│  ┌────────────────────────────────────┐    │
│  │  1. CORS + JSON Parser + Logger     │    │
│  └────────────┬───────────────────────┘    │
│               │                             │
│  ┌────────────▼───────────────────────┐    │
│  │  2. Route: /api/bookings            │    │
│  └────────────┬───────────────────────┘    │
│               │                             │
│  ┌────────────▼───────────────────────┐    │
│  │  3. verifyToken Middleware          │    │
│  │     • Extract Bearer token          │    │
│  │     • Verify with Firebase Admin    │◄───┼────┐
│  │     • Get user from MongoDB         │    │    │
│  │     • Attach to req.user            │    │    │
│  └────────────┬───────────────────────┘    │    │
└───────────────┼─────────────────────────────┘    │
                │                                   │
                ▼                                   │
┌──────────────────────────────────┐          ┌────▼────────┐
│  Firebase Admin SDK              │          │   MongoDB   │
│  verifyIdToken()                 │          │   users     │
└──────────────────────────────────┘          └─────────────┘
                │
                │ [Token valid + User data]
                │
                ▼
┌───────────────────────────────────────┐
│  bookingController.createBooking()    │
│  • Validate input                     │
│  • Get hotel info                     │
│  • Generate booking number            │
│  • Create booking in DB               │
└──────────┬────────────────────────────┘
           │
           ▼
┌──────────────────────┐
│   MongoDB Atlas      │
│   Insert: bookings   │
└──────────┬───────────┘
           │
           │ [New booking document]
           │
           ▼
┌────────────────────────┐
│   Response             │
│   {                    │
│     success: true,     │
│     message: "...",    │
│     data: {...}        │
│   }                    │
└──────────┬─────────────┘
           │
           ▼
     ┌─────────┐
     │ Client  │
     └─────────┘
```

## 🔐 Authentication Flow

```
┌──────────────────────────────────────────────────────────┐
│                    REGISTRATION                           │
└──────────────────────────────────────────────────────────┘

Frontend                Backend                  Firebase      MongoDB
   │                       │                         │            │
   │ POST /auth/register   │                         │            │
   │──────────────────────>│                         │            │
   │  {email, password}    │                         │            │
   │                       │                         │            │
   │                       │ admin.auth()            │            │
   │                       │   .createUser()         │            │
   │                       │────────────────────────>│            │
   │                       │                         │            │
   │                       │<────────────────────────│            │
   │                       │  {uid, email}           │            │
   │                       │                         │            │
   │                       │  new User({uid...})     │            │
   │                       │─────────────────────────────────────>│
   │                       │                         │            │
   │                       │<─────────────────────────────────────│
   │                       │  User saved             │            │
   │                       │                         │            │
   │<──────────────────────│                         │            │
   │  {success, data}      │                         │            │
   │                       │                         │            │

┌──────────────────────────────────────────────────────────┐
│                    LOGIN                                  │
└──────────────────────────────────────────────────────────┘

Frontend                Backend                  Firebase      MongoDB
   │                       │                         │            │
   │ Firebase SDK          │                         │            │
   │ signInWith            │                         │            │
   │ EmailAndPassword()    │                         │            │
   │──────────────────────────────────────────────>│            │
   │                       │                         │            │
   │<──────────────────────────────────────────────│            │
   │  {user, token}        │                         │            │
   │                       │                         │            │
   │ POST /auth/login      │                         │            │
   │ {token}               │                         │            │
   │──────────────────────>│                         │            │
   │                       │                         │            │
   │                       │ admin.auth()            │            │
   │                       │   .verifyIdToken()      │            │
   │                       │────────────────────────>│            │
   │                       │                         │            │
   │                       │<────────────────────────│            │
   │                       │  {uid, email}           │            │
   │                       │                         │            │
   │                       │  findOne({uid})         │            │
   │                       │─────────────────────────────────────>│
   │                       │                         │            │
   │                       │<─────────────────────────────────────│
   │                       │  User data              │            │
   │                       │                         │            │
   │<──────────────────────│                         │            │
   │  {success, user}      │                         │            │
   │                       │                         │            │
```

## 💾 Database Schema Relationships

```
┌─────────────────────────┐
│        User             │
│─────────────────────────│
│ _id: ObjectId          │
│ uid: String (Firebase) │◄───────┐
│ userName: String       │        │
│ email: String          │        │
│ phone: String          │        │
│ avatar: String         │        │
│ favorites: [ObjectId]  │──┐     │
└─────────────────────────┘  │     │
         │                   │     │
         │ 1:N               │     │
         │                   │     │
         ▼                   │     │
┌─────────────────────────┐  │     │
│       Booking           │  │     │
│─────────────────────────│  │     │
│ _id: ObjectId          │  │     │
│ bookingNumber: String  │  │     │
│ userId: ObjectId       │──┘     │
│ hotelId: Number        │        │
│ checkIn: Date          │        │
│ checkOut: Date         │        │
│ guests: Number         │        │
│ totalPrice: Number     │        │
│ status: String         │        │
└─────────────────────────┘        │
         │                         │
         │ N:1                     │
         │                         │
         ▼                         │
┌─────────────────────────┐        │
│        Hotel            │◄───────┘ (Favorites)
│─────────────────────────│
│ _id: ObjectId          │
│ id: Number             │
│ name: String           │
│ location: String       │
│ price: Number          │
│ rating: Number         │
│ photos: [String]       │
│ roomTypes: [Room]      │──┐
│ reviews: [Review]      │  │ Embedded
└─────────────────────────┘  │
         │                   │
         │ 1:N               │
         │                   │
         ▼                   │
┌─────────────────────────┐  │
│       Review            │  │
│─────────────────────────│  │
│ _id: ObjectId          │  │
│ userId: ObjectId       │──┼──► User
│ hotelId: ObjectId      │──┘
│ rating: Number         │
│ comment: String        │
└─────────────────────────┘

┌─────────────────────────┐
│        Room             │
│─────────────────────────│
│ _id: ObjectId          │
│ name: String           │
│ type: String           │
│ price: Number          │
│ capacity: Number       │
│ images: [String]       │
└─────────────────────────┘
```

## 🛣️ API Routes Map

```
/api
│
├── /auth
│   ├── POST   /register              [Public]
│   ├── POST   /login                 [Public]
│   ├── GET    /me                    [Private]
│   └── POST   /logout                [Private]
│
├── /hotels
│   ├── GET    /                      [Public]
│   ├── GET    /search/:location      [Public]
│   ├── GET    /:id                   [Public]
│   ├── GET    /:id/reviews           [Public]
│   ├── POST   /                      [Private/Admin]
│   ├── PUT    /:id                   [Private/Admin]
│   └── DELETE /:id                   [Private/Admin]
│
├── /bookings
│   ├── GET    /                      [Private/Admin]
│   ├── GET    /my-bookings           [Private]
│   ├── GET    /stats                 [Private/Admin]
│   ├── GET    /:id                   [Private]
│   ├── POST   /                      [Private]
│   ├── PUT    /:id                   [Private]
│   └── DELETE /:id                   [Private]
│
├── /reviews
│   ├── GET    /                      [Public]
│   ├── GET    /hotel/:hotelId        [Public]
│   ├── GET    /user/:userId          [Public]
│   ├── GET    /:id                   [Public]
│   ├── POST   /                      [Private]
│   ├── PUT    /:id                   [Private]
│   └── DELETE /:id                   [Private]
│
└── /users
    ├── GET    /                      [Private/Admin]
    ├── GET    /:id                   [Private]
    ├── PUT    /:id                   [Private]
    ├── DELETE /:id                   [Private]
    ├── GET    /:id/favorites         [Private]
    ├── POST   /:id/favorites/:hotelId    [Private]
    └── DELETE /:id/favorites/:hotelId    [Private]
```

## 🔧 Middleware Stack

```
Request
   ↓
┌──────────────────────────┐
│  CORS Middleware         │  ← Allow cross-origin requests
└────────┬─────────────────┘
         │
┌────────▼─────────────────┐
│  Express JSON Parser     │  ← Parse JSON body
└────────┬─────────────────┘
         │
┌────────▼─────────────────┐
│  Logger Middleware       │  ← Log requests (dev only)
└────────┬─────────────────┘
         │
┌────────▼─────────────────┐
│  Route Handler           │  ← Match URL to route
└────────┬─────────────────┘
         │
┌────────▼─────────────────┐
│  verifyToken (optional)  │  ← Check authentication
└────────┬─────────────────┘
         │
┌────────▼─────────────────┐
│  Controller Function     │  ← Execute business logic
└────────┬─────────────────┘
         │
         ├─── Success
         │    └─── Response
         │
         └─── Error
              ↓
┌──────────────────────────┐
│  Error Handler           │  ← Format error response
└────────┬─────────────────┘
         │
    Response
```

## 📊 Status Codes Guide

```
2xx Success
├── 200 OK                   → GET, PUT requests
├── 201 Created              → POST requests
└── 204 No Content           → DELETE requests

4xx Client Errors
├── 400 Bad Request          → Validation errors
├── 401 Unauthorized         → No/invalid token
├── 403 Forbidden            → Not authorized
└── 404 Not Found            → Resource not found

5xx Server Errors
└── 500 Internal Server Error → Unexpected errors
```

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────┐
│              Development                         │
│  ┌──────────┐    ┌──────────┐                  │
│  │ VS Code  │───►│localhost │                  │
│  │          │    │  :8080   │                  │
│  └──────────┘    └────┬─────┘                  │
└────────────────────────┼───────────────────────┘
                         │
                    git push
                         │
                         ▼
┌─────────────────────────────────────────────────┐
│              GitHub                              │
│  ┌────────────────────────────────────────┐    │
│  │  Repository: hotel-book-me             │    │
│  │  Branch: backend                       │    │
│  └────────────────────────────────────────┘    │
└────────────────────┬───────────────────────────┘
                     │
              Auto-deploy trigger
                     │
                     ▼
┌─────────────────────────────────────────────────┐
│              Render.com                          │
│  ┌────────────────────────────────────────┐    │
│  │  1. npm install                        │    │
│  │  2. npm start                          │    │
│  │  3. Health check                       │    │
│  └──────────┬─────────────────────────────┘    │
│             │                                   │
│  ┌──────────▼─────────────────────────────┐    │
│  │  https://hotel-booking-api.           │    │
│  │          onrender.com                  │    │
│  └────────────────────────────────────────┘    │
└────────────────┬────────────────┬───────────────┘
                 │                │
        ┌────────▼────┐    ┌─────▼──────┐
        │  MongoDB    │    │  Firebase  │
        │   Atlas     │    │   Admin    │
        └─────────────┘    └────────────┘
```

## 💡 Quick Commands

```bash
# Installation
npm install

# Development
npm run dev          # Run with nodemon (auto-restart)

# Production
npm start            # Run with node

# Testing
curl http://localhost:8080/
curl http://localhost:8080/api/hotels

# Git
git add .
git commit -m "message"
git push origin backend
```

---

**Legend:**

-   🚀 Entry point
-   🔐 Authentication
-   🏨 Hotel management
-   📅 Booking
-   ⭐ Reviews
-   👤 User
-   🛡️ Security
-   📦 Data
-   🛣️ Routes
-   📚 Documentation
